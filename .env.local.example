# Local development overrides. Copy to .env.local.
# This file WINS over .env.production, so do not leave it pinning localhost on a
# machine that also builds for production.
NEXT_PUBLIC_API_URL=http://localhost:5200
# seconds the site caches API data; 0 = always fresh (use while editing in the admin)
REVALIDATE_SECONDS=0
